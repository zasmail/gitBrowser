{{#if isDynamic}}
  <button class="dynamic-segment" {{action 'startInputting' on="click"}}>
    {{#if isInputting}}
      {{input type="text" class="form-control" value=segment}}
      <button class="dynamic-segment" {{action 'stopInputting' bubbles=false}}>okay</button>
    {{else}}
      {{~segment~}}
    {{/if}}
  </button>
{{else}}
  {{~segment~}}
{{/if}}
